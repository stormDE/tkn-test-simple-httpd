# Default values for tekton-test.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Section Values for Pipeline starts here
pipeline:
  enabled: true
  helloWorld:
    appendix: st0rm

# Section Values for Deployment starts here
deployment:
  replicaCount: 1
  enabled: true
image:
  repository: registry.access.redhat.com
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  path: rhscl/httpd-24-rhel7
  tag: "latest"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext:
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
    ## oc get namespace NAMESPACE -o yaml | grep uid-range
  # runAsUser:

service:
  type: ClusterIP
  port: 8080

route:
  enabled: true
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  host: apache-tekton-test.apps-crc.testing
  tls:
    enabled: true

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}
